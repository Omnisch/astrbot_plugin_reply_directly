# astrbot_plugin_reply_directly/_conf_schema.json
{
  "follow_up_delay": {
    "description": "机器人发送消息后，等待多少秒后触发追问检查。",
    "type": "int",
    "default": 5,
    "hint": "单位为秒。机器人会在此延迟后，结合上下文和自己刚发的消息，思考是否需要进行补充回答。"
  }
}
